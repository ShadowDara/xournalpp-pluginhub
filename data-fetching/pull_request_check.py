import os
import sys
import json
from pathlib import Path
from dataclasses import dataclass
from typing import List, Optional
import requests


print("Make an Pull Request")

print("This is not fine yet, but it will be soon!")

# check if the namespace is the same as the name of the registration file

sys.exit(1)
